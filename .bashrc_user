#!/bin/bash

[[ -f "$HOME/.nix-profile/etc/profile.d/nix.sh" ]] && \
    . $HOME/.nix-profile/etc/profile.d/nix.sh

# Start sway only if gdm is not used
type -p gdm &> /dev/null || { [[ "$(tty)" = "/dev/tty1" ]] && ssh-agent sway; }
