#!/bin/bash

[[ $SHELL = "/nix/"* ]] && return
[[ -f ~/norc ]] && return

# user environment variables
export PATH="$PATH:$HOME/scripts:$HOME/.node/bin:$HOME/.cargo/bin"
export NODE_PATH="$NODE_PATH:$HOME/.node/lib/node_modules"
export EDITOR="vim"
export TERMINAL="$HOME/st/st"
export PREFIXPATH="$HOME/storage/prefix"
export SRCPATH="$HOME/storage/src"

if [[ ! -z "$PS1" ]] && [[ -n "$BASH_VERSION" ]]; then
    # only for bash
    bind '"\e[1;5D" backward-word'
    bind '"\e[1;5C" forward-word'
    bind '"\eo":"cd-from-ranger\C-m"'
    bind '"\et":"tmux\C-m"'
    eval "$(fasd --init auto)"
fi

# load helper scripts
[[ -f ~/.bashrc_functions ]] && . ~/.bashrc_functions
[[ -f ~/.bashrc_aliases ]] && . ~/.bashrc_aliases
[[ -f ~/.bashrc_stage ]] && . ~/.bashrc_stage
